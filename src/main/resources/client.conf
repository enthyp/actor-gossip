akka {
  actor {
    provider = remote
    warn-about-java-serializer-usage = false
  }
}

akka {
  remote {
    enabled-transports = ["akka.remote.netty.tcp"]
    netty.tcp {
      hostname = ""
      port = 0
    }
  }
}
